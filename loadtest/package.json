{
  "name": "m2c-boat-trip-loadtest",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright load harness. Kept in its own package so the deployed app has zero dependencies.",
  "scripts": {
    "postinstall": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  },
  "license": "UNLICENSED"
}
