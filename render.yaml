services:
  - type: web
    name: m2c-boat-trip
    runtime: node
    plan: free
    buildCommand: "echo no build step required"
    startCommand: "node server.js"
    healthCheckPath: /healthz
    envVars:
      - key: NODE_VERSION
        value: "22"
